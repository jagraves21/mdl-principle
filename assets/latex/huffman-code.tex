\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{trees,positioning}

\usepackage{mytikzstyles}

\begin{document}

\begin{tikzpicture}[mytree,line width=0.75pt]

\node {$\varepsilon$}
	child [chosen] { node [codeword, label={[shift={(0em,-0.25em)}]$a$}] {0}
		child { node {00}
			child { node {000}
				child { node {$\vdots$} }
				child { node {$\vdots$} }
			}
			child { node {001}
				child { node {$\vdots$} }
				child { node {$\vdots$} }
			}
		}
		child { node {01}
			child { node {010}
				child { node {$\vdots$} }
				child { node {$\vdots$} }
			}
			child { node {011}
				child { node {$\vdots$} }
				child { node {$\vdots$} }
			}
		}
	}
	child { node {1}
		child { node {10}
			child [chosen] { node [codeword, label={[shift={(0em,-0.25em)}]$b$}] {100}
				child { node {$\vdots$} }
				child { node {$\vdots$} }
			}
			child [chosen] { node [codeword, label={[shift={(0em,-0.25em)}]$c$}] {101}
				child { node {$\vdots$} }
				child { node {$\vdots$} }
			}
		}
		child { node {11}
			child [chosen] { node [codeword, label={[shift={(0em,-0.25em)}]$d$}] {110}
				child { node {$\vdots$} }
				child { node {$\vdots$} }
			}
			child [chosen] { node [codeword, label={[shift={(0em,-0.25em)}]$e$}] {111}
				child { node {$\vdots$} }
				child { node {$\vdots$} }
			}
		}
	};

\end{tikzpicture}

\end{document}

